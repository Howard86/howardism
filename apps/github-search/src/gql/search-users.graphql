query searchUsers($query: String!, $count: Int!, $startAt: String) {
  search(query: $query, type: USER, first: $count, after: $startCursor) {
    nodes {
      ... on User {
        login
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
